export const metadata = {
  title: "Comparison Tables — Mariana Difini",
  description:
    "Case study: redesigning Qonto comparison tables for clarity, SEO, and user confidence.",
};

export default function ComparatorsTableLayout({ children }) {
  return children;
}
